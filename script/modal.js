const modalWindowWrapper = document.createElement("div");
modalWindowWrapper.className = "modalWindowWrapper";

const modalWindowContainer = document.createElement("div");
modalWindowContainer.className = "modalWindowContainer";

const alertModalWindow = document.createElement("div");
alertModalWindow.className = "alertModalWindow";

const alertMessageText = document.createElement("h2");
alertMessageText.className = "alertMessage";

const agreeBtn = document.createElement("button");
agreeBtn.className = "agreeBtn";
agreeBtn.innerText = "Ok";